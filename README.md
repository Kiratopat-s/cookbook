# Cookbook Knowledge

A static GitHub Pages cookbook where each `.html` file is a complete, self-contained recipe or cooking note.

## Structure

- `index.html` is the cookbook index.
- New pages should use lowercase kebab-case filenames.
- Each page should contain its own content and page-specific styling.

## Publishing

GitHub Pages can publish this repository directly as a static site. No build step is required.
